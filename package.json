{
  "name": "lootradar",
  "version": "2.0.0",
  "private": true,
  "description": "Quality-first PC game deal finder",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "build": "node scripts/build-weekly-guide.js && node scripts/build-game-pages.js && node scripts/build-guide-deal-modules.js && node scripts/build-search-pages.js && node scripts/build-home-fallback.js && node scripts/generate-sitemap.js && node scripts/build-static.js",
    "verify": "node scripts/verify-site.js",
    "brand:build": "node scripts/generate-brand-assets.js",
    "guides:build": "node scripts/build-weekly-guide.js && node scripts/build-guide-deal-modules.js && node scripts/generate-sitemap.js",
    "guides:check": "node scripts/build-weekly-guide.js --check",
    "guides:candidates": "node scripts/list-weekly-candidates.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "png-to-ico": "^3.0.2",
    "sharp": "^0.35.3"
  }
}
